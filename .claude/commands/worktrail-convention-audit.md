# Convention Audit

Scan the knowledge base for convention violations

Read `skills/convention-audit.md` and execute it against the current knowledge base. Pass any arguments from `$ARGUMENTS` as the `scope` input if provided.
